---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: moria-ingress
  namespace: industries
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: traefik
  rules:
    - host: auth.julian-one.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: moria
                port:
                  number: 80
  tls:
    - hosts:
        - auth.julian-one.com
      secretName: moria-tls
