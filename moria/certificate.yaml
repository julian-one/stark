---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: moria-tls
  namespace: industries
spec:
  secretName: moria-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  commonName: auth.julian-one.com
  dnsNames:
    - auth.julian-one.com
